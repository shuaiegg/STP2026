## 1. 注册奖励积分更新

- [x] 1.1 在 `src/lib/auth.ts` 的 `databaseHooks.user.create.after` 钩子中，将积分增量从 `5` 改为 `10`（同步修改 `amount: 5` → `amount: 10` 以及 `increment: { credits: 5 }` → `increment: { credits: 10 }`）

## 2. 定价页面改版

- [x] 2.1 删除 `src/app/(public)/pricing/page.tsx` 中的 `getFeatures()` 函数
- [x] 2.2 删除 `getDescription()` 函数，将各套餐描述内联至套餐卡片或改为统一描述
- [x] 2.3 将套餐卡片中的功能列表区块替换为基于积分数量的价值说明：50积分 → "可完成约 3 篇深度内容创作 或 10 次站点体检"，130积分 → "可完成约 8 篇深度内容创作 或 26 次站点体检"，300积分 → "可完成约 20 篇深度内容创作 或 60 次站点体检"
- [x] 2.4 在套餐网格下方新增"积分消耗参考"区块，以卡片或表格形式展示：StellarWriter 智作（15 积分/篇）、站点 SEO 体检（5 积分/次），并注明"所有功能对所有积分持有者平等开放"
- [x] 2.5 确认套餐卡片不再展示任何套餐间差异化的功能列表，所有卡片访问权限描述一致
